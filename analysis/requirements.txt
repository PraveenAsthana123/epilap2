# Scientific stack for the epilepsy analytics pipelines (primary + secondary + fusion).
# Versions are lower bounds verified against Python 3.13.
numpy>=2.0
pandas>=2.2
scipy>=1.13
statsmodels>=0.14
matplotlib>=3.8
scikit-learn>=1.5
